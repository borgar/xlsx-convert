export type ManualLayout = {
  layoutTarget?: 'inner' | 'outer';
  xMode?: 'edge' | 'factor';
  yMode?: 'edge' | 'factor';
  wMode?: 'edge' | 'factor';
  hMode?: 'edge' | 'factor';
  x?: number;
  y?: number;
  w?: number;
  h?: number;
};
